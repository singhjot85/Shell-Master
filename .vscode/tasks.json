{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "Poetry Debug Python",
        "type": "shell",
        "command": "poetry run python -c \"import debugpy; debugpy.listen(('localhost', 5678)); print('✅ debugpy listening on port 5678'); import code; code.interact(local=globals())\"",
        "problemMatcher": [],
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }
]
}
